import * as actionTypes from "../actions/types";

const initialState = {
  items: []
};

export default reducer = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
};
